import 'package:flutter/material.dart';

class ATHSliverCardWrapper extends StatelessWidget {
  final Widget child;

  ATHSliverCardWrapper({this.child});

  @override
  Widget build(BuildContext context) {}
}
